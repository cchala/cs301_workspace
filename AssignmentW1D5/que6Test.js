"use strict";

const assert = require("assert");
const my_functions = require('./que6.js');

describe("checkPrime", function () {
    it(" least common multiple:", function () {
        assert.strictEqual(my_functions.add(2,2), 4);
    });
    it("Least common Multiple :", function () {
        assert.strictEqual(my_functions.compute(add,3,2),7);
    });
    it(" least common multiple:", function () {
        assert.strictEqual(my_functions.compute(add,2,2), 4);
    });
   
});